# Build output. scripts/zig.sh keeps both caches in a docker volume instead, so
# neither should ever appear here at all.
.zig-cache/
zig-cache/
zig-out/
# Where zig 0.16 unpacks a fetched dependency, which integration/scripts/run.sh
# does on every run.
zig-pkg/
